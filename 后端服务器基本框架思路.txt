1. 底层采用socket通信，服务器接收浏览器发来的请求，所以在服务器端将请求封装成类，方便处理，
封装为Request类，实例属性有method,path,query,body
2. 主程序开启监听后在accept()处阻塞，直到接收到浏览器请求
3. 接收到浏览器请求后，将原始请求分解到实例属性中，为后续路由函数中后端逻辑处理做准备
4. 通过请求中的path字段来选择路由函数
5. 在路由函数中实现后端逻辑(操作数据库，判断等)，并返回页面
6. 服务器通过sendall()将路由函数返回的页面作为响应传送到浏览器客户端